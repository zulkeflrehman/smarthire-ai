# Render production requirements — Python 3.11
# spaCy excluded (Cython compile fails on free tier)
# sentence-transformers excluded (512MB RAM limit — falls back to TF-IDF retrieval)
# groq INCLUDED — pure Python HTTP wrapper, no compilation

Django==4.2.7
gunicorn==21.2.0
whitenoise==6.6.0
python-decouple==3.8
psycopg2-binary==2.9.9
dj-database-url==2.1.0
pdfplumber==0.10.3
python-docx==1.1.0
scikit-learn==1.4.2
numpy==1.26.4
groq==1.5.0
httpx>=0.27.0,<0.28.0
